- name: Print Variables from Files
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: Task to print variables from Files
    ansible.builtin.debug:
      msg: "Course is {{COURSE}}, Trainer is {{TRAINER}}, Duration is {{DURATION}}"
